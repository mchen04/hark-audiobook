# dependencies
/node_modules/

# Next.js
/.next/
/out/

# local runtime data and generated media
/.data/
/uploads/
*.mp3
!tests/fixtures/Downloads/Chapterline-iPhone-Test.mp3
# Tiny (2-chapter) generated MP3 carrying an embedded read-along transcript,
# used by the import/privacy tests to exercise the real GEOB parse path.
!tests/fixtures/transcripts/tiny-book.mp3

# environment and secrets
.env
.env.*
!.env.example
# .env.test holds only local-container credentials, but keep it out of the repo
# so a stray hosted connection string can never be committed through it.
!.env.test.example

# generated service worker artifacts
/public/sw.js.map
/public/pdf.worker.min.mjs
/public/models/kestrel/kissfft.wasm

# test and coverage output
/coverage/
/test-results/
/playwright-report/

# editor, agent, and OS files
.DS_Store
*.log
/*-loop.log.md
.vscode/
.idea/
.claude/
scratchpad/

# TypeScript
*.tsbuildinfo
next-env.d.ts
.vercel
